[project]
name = "courtvision-capture-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
vision = ["ultralytics==8.4.138"]

[project.scripts]
courtvision-edge = "main:main"
