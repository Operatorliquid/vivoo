services:
  evolution-api:
    image: evoapicloud/evolution-api:v2.3.7
    container_name: courtvision_evolution_api
    restart: unless-stopped
    depends_on:
      evolution-postgres:
        condition: service_healthy
      evolution-redis:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - evolution_instances:/evolution/instances
    networks:
      - courtvision-messaging

  evolution-postgres:
    image: postgres:15-alpine
    container_name: courtvision_evolution_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: evolution
      POSTGRES_USER: evolution
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U evolution -d evolution"]
      interval: 5s
      timeout: 5s
      retries: 12
    volumes:
      - evolution_postgres:/var/lib/postgresql/data
    networks:
      - courtvision-messaging

  evolution-redis:
    image: redis:7-alpine
    container_name: courtvision_evolution_redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12
    volumes:
      - evolution_redis:/data
    networks:
      - courtvision-messaging

volumes:
  evolution_instances:
  evolution_postgres:
  evolution_redis:

networks:
  courtvision-messaging:
    name: courtvision-messaging
